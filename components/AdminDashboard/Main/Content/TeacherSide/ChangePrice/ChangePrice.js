import Box from "../../Elements/Box/Box";

function ChangePrice({ languages, token }) {
    console.log(languages);
    return (
        <div>
            <Box title="لیست قیمت زبان های استاد">س</Box>
        </div>
    );
}

export default ChangePrice;
